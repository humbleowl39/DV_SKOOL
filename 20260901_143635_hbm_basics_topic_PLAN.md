# Plan: `hbm_basics` — HBM 기초 토픽 신설

## Objective

**HBM이 무엇이고 어떻게 동작하는가**를 처음부터 설명하는 입문 토픽을 만든다.
DRAM 배경이 없는 독자가 `hbm` 코스로 들어가기 전에 밟는 계단이며,
현재 어느 토픽에도 통으로 없는 **물리 계층(TSV·µbump·인터포저)과 한 트랜잭션의 전체 여정**을 담는다.

## Context — 왜 필요한가 (조사 결과)

### 현재 HBM 토픽에 없는 것

| 항목 | 현황 |
|---|---|
| DRAM 셀 동작 원리 | `hbm` 토픽에 **0건** (`커패시터`·`센스앰프`·`파괴적` 검색 결과 0). `dram_ddr/01` §5.1이 정본 |
| **TSV 동작 원리** | 용어집 정의 + `hbm/01`의 지나가는 언급뿐. **어떻게 신호가 지나가는지 설명 없음** |
| **마이크로범프·인터포저** | `hbm/01`에 "인터포저 위 마이크로미터 거리" 수준 언급만 |
| **호스트→셀 전체 경로** | **전 토픽에 0건.** 데이터가 물리적으로 어디를 지나는지 통으로 서술한 곳이 없음 |
| 열·발열 | `hbm/01` 1개 파일에만 언급 |

### 기존 토픽의 링크 실태

`hbm` 토픽은 index에서 *"DRAM 일반론을 반복하지 않고 HBM이 다른 지점만 다룬다"* 고 선언하지만,
본문 6장의 `dram_ddr` 링크는 **`01`·`04`에 각 1건뿐**. 기초가 가장 필요한 `03`·`05`·`06`에는 **0건**.

### ⚠️ 중복 위험 — 사용자에게 이미 고지한 사항

`dram_ddr/01` §5.1은 DRAM 셀을 **매우 깊게** 다룬다 — 1T1C 구조, charge sharing, cross-coupled latch,
정/부귀환, 그리고 그것이 `tRCD`·`tRAS`의 물리적 뿌리인 이유까지. **이것을 다시 쓰면 안 된다.**

**중복 회피 원칙 (이 계획의 핵심 제약)**

| 주제 | 정본 | `hbm_basics`에서의 취급 |
|---|---|---|
| DRAM 셀 물리 (1T1C·센스앰프·charge sharing) | `dram_ddr/01` §5.1 | **1문단 요약 + 링크.** 재서술 금지 |
| LPDDR5 고유 기능 (WCK2CK·PASR·DVFSC) | `dram_ddr` | **다루지 않음** |
| 메모리 컨트롤러 스케줄링 | `dram_ddr/02` | **다루지 않음** |
| HBM 검증 문제 제기 | `hbm` | **다루지 않음** (기초는 "무엇/어떻게"까지) |
| 규격 조문 | `hbm4_jedec_dd` | **다루지 않음** |
| **TSV·µbump·인터포저 동작** | — | **이 토픽이 정본** |
| **호스트→셀 전체 경로** | — | **이 토픽이 정본** |
| **적층의 열·전력 귀결** | — | **이 토픽이 정본** |

## 학습 사다리에서의 위치

```
[신규] hbm_basics   HBM이 무엇이고 어떻게 동작하는가        ← 입문
   ↓
hbm                 검증 대상으로서의 HBM 구조              (문제 제기)
   ↓
hbm_dv              검증 환경을 어떻게 짜는가              (방법론)
   ↓
hbm4_jedec_dd       규격이 무엇을 검증하라고 요구하는가     (조문 → 검증 항목)
```

`dram_ddr`는 **병렬 선수 과목** — HBM 기초는 DRAM 원리를 요약·링크하고, 깊이는 저기로 보낸다.

## 구성안 — 7장 + 용어집 + 퀴즈

기존 토픽 관례를 따른다 (전 토픽이 `quiz/` + `glossary.md` 보유, 장당 300~350줄).

| 장 | 제목 | 답하는 질문 | 중복 회피 |
|---|---|---|---|
| **01** | 메모리는 왜 느린가 | 프로세서와 메모리 사이에서 무엇이 병목인가. DRAM 셀·뱅크·행·열을 **HBM 이해에 필요한 만큼만** | 셀 물리는 `dram_ddr/01` 링크 |
| **02** | 대역폭을 늘리는 두 가지 방법 | 폭 × 속도. 속도를 올리면 왜 막히는가 (배선 길이·에너지·SI) | `hbm/01`과 겹칠 위험 → **`hbm/01`은 "검증 문제"로 착지, 여기는 "물리"로 착지** |
| **03** | 쌓는다는 것 — TSV | TSV가 무엇이고 신호가 어떻게 지나가는가. 왜 이제야 가능해졌나 | **신규 정본** |
| **04** | 잇는다는 것 — µbump와 인터포저 | 호스트와 HBM은 물리적으로 어떻게 연결되는가. 2.5D의 의미 | **신규 정본** |
| **05** | 스택의 해부 | core die × N + base die. 채널이 스택을 어떻게 가로지르는가 | `hbm/03`은 **DUT 경계**, 여기는 **물리 구조** |
| **06** | 한 번의 읽기, 그 전체 여정 ★ | 호스트 요청 → PHY → 인터포저 → µbump → base die → TSV → core die → 뱅크 → 셀 → 되돌아오기 | **신규 정본. 이 토픽의 척추** |
| **07** | 쌓으면 뜨거워진다 | 열이 어디서 나고 어디로 빠지나. refresh·성능·수명에 미치는 영향 | **신규 정본** |

### 각 장의 공통 구조

`hbm` 토픽과 같은 골격을 쓰되 **착지점만 다르다** — `hbm`은 "검증 문제"로, 여기는 "그래서 다음 장에서 무엇을 보나"로.

```
1. Why care? — 구체적 상황에서 출발
2. 직관 — 비유와 한 장 그림 (d2)
3. 작은 예 — 숫자로 따라가기
4. 일반화 — 왜 이 형태인가
5. 디테일 — 실제로는 이렇게 복잡하다
6. 흔한 오해
7. 핵심 정리 / 다음 장
```

## Steps

### Phase 0 — 골격
- [ ] S0. 디렉토리 생성 + `index.mdx` (위치·학습목표·사전지식·중복 고지·챕터 카드)
- [ ] S1. 사이드바(`astro.config.mjs`)에 `hbm_basics` 추가 — 메모리 그룹, `dram_ddr` 다음·`hbm` 앞
- [ ] S2. 최상위 `index.mdx` 토픽 카드 추가

### Phase 1 — 본문 (장당 1 micro-step)
- [ ] S3. 01 메모리는 왜 느린가
- [ ] S4. 02 대역폭을 늘리는 두 가지 방법
- [ ] S5. 03 쌓는다는 것 — TSV
- [ ] S6. 04 잇는다는 것 — µbump와 인터포저
- [ ] S7. 05 스택의 해부
- [ ] S8. 06 한 번의 읽기, 그 전체 여정 ★
- [ ] S9. 07 쌓으면 뜨거워진다

### Phase 2 — 부속
- [ ] S10. `glossary.md` (ISO 11179 형식, 기존 토픽 관례)
- [ ] S11. `quiz/` 7편 + `quiz/index.md` (Bloom 혼합, 장당 6문항)

### Phase 3 — 통합·검증
- [ ] S12. 기존 토픽 역링크 — `hbm/index`·`hbm_dv/index`·`dram_ddr/index`의 사다리 표에 추가.
       `hbm` 본문 `03`·`05`·`06`의 기초 링크 공백도 함께 메움 (앞서 지적한 문제)
- [ ] S13. d2 SVG 생성 (`gen_d2.mjs --topic hbm_basics`) + 겹침 검사
- [ ] S14. `CI=true astro build` + 링크 검증 + 커밋·푸시
- [ ] S15. 계획서 closeout

## Success Criteria

1. `dram_ddr/01` §5.1의 셀 물리 서술과 **중복 0** — 요약 1문단 + 링크로만 처리
2. `hbm/01`과 02장이 **다른 착지점**을 가짐 (물리 vs 검증 문제)
3. TSV·µbump·인터포저·전체 경로·열 — **5개 신규 주제**가 실제로 새 내용
4. 7장 전부 d2 다이어그램 최소 1개, 라벨 겹침 0
5. 용어집 + 퀴즈 7편 구비 (기존 토픽 관례)
6. `CI=true astro build` 통과, 링크 깨짐 0
7. 사다리 4단(`hbm_basics` → `hbm` → `hbm_dv` → `hbm4_jedec_dd`)이 전 토픽 index에서 일관되게 서술됨
8. 코드 샘플에 `$display`/`$finish`/`$stop` 없음 (해당 시)

## Risks / Open Questions

**R-1. `hbm/01`과 02장의 중복** — 둘 다 "대역폭을 왜 폭으로 푸는가"를 다룬다.
→ 착지를 가른다. `hbm/01`은 *"그래서 검증이 SoC 검증처럼 생겼다"*, `hbm_basics/02`는 *"그래서 쌓아야 한다 → 03장 TSV"*.
02장 말미에 `hbm/01`로의 링크를 두어 독자가 두 번 읽지 않게 한다.

**R-2. 물리·공정 영역 침범** — `hbm/index`는 *"Wafer Bonding·Thinning·BEOL 같은 공정 상세는 다루지 않는다"* 고 선언했다.
→ `hbm_basics`도 **같은 선을 지킨다.** TSV를 다루되 *"어떻게 만드는가"*(공정)가 아니라 *"어떻게 동작하는가"*(전기·구조)까지.

**R-3. 이미지 자산** — `dram_ddr`는 외부 CC 이미지를 씀(`img/dram_1t1c_cell.svg`).
→ `hbm_basics`는 **d2 다이어그램만** 사용한다. 저작권 문제를 만들지 않고, 사이트의 d2 파이프라인을 그대로 쓴다.

**R-4. 분량** — 7장 × 약 330줄 ≈ 2,300줄 + 용어집·퀴즈 약 1,200줄 ≈ **3,500줄**.
`hbm` 토픽(2,031줄)보다 크다. micro-step으로 장 단위 승인.

### 결정 (2026-09-01 확정)

**Q1 → A.** 디렉토리 `hbm_basics`, 사이드바 라벨 **"HBM 기초"**.

**Q2 → B.** DRAM 기초를 **독립 장으로 확장.** `dram_ddr` 없이도 완결되게 한다.

**Q2-B에 따른 구성 변경**

01장이 DRAM 기초를 통째로 차지하므로, 원래의 01(메모리는 왜 느린가)과 02(대역폭 두 갈래)가 **02장으로 합쳐진다.** 7장은 유지.

| 장 | 변경 후 제목 |
|---|---|
| 01 | **DRAM은 어떻게 동작하는가** (신규 확장 — 셀·뱅크·행/열·ACT/PRE/REF, 자족적) |
| 02 | **왜 넓은 메모리가 필요한가** (Memory Wall + 대역폭 두 갈래 통합) |
| 03~07 | 변경 없음 |

**Q2-B의 대가와 완화책**

`dram_ddr/01` §5.1과 사실 관계가 겹친다. 사용자에게 고지했고 B를 선택했으므로 진행하되, **조직과 깊이 프로파일을 다르게** 해 단순 복제를 피한다.

| | `hbm_basics/01` | `dram_ddr/01` §5.1 |
|---|---|---|
| 서술 축 | 동작의 **흐름** (열고·읽고·닫고·되살린다) | 동작의 **물리** (charge sharing → latch → full rail) |
| 깊이 | HBM으로 가기에 충분한 만큼 | 소자 수준까지 |
| 착지 | "그래서 타이밍 파라미터가 생긴다" → 02장 | "그래서 `tRCD`·`tRAS`가 존재한다" → LPDDR5 |
| 물리 심층 | **`dram_ddr/01`로 링크** | 정본 |

index.mdx에 두 코스의 관계표를 명시해 독자가 어디로 가야 할지 알 수 있게 했다.

---

# Closeout (2026-09-01)

## 결과

| 커밋 | 범위 |
|---|---|
| `3c93a39` | index·사이드바·최상위 카드 + 본문 01~04장 **+ d2 예약어 결함 6건 수정** |
| `ace5476` | 본문 05~07장 · 용어집 · 퀴즈 7편 · 역링크 보강 |

**전 15스텝 완료.** Q1-A(`hbm_basics` / "HBM 기초"), Q2-B(DRAM 기초를 독립 장으로) 반영.

## 산출물

| 항목 | 분량 |
|---|---|
| 본문 7장 | 2,124줄 |
| `index.mdx` | 108줄 |
| `glossary.md` | 382줄 · **26개 항목** (ISO 11179 5필드) + 타이밍 8개 표 |
| 퀴즈 7편 + index | 992줄 · **42문항** |
| d2 다이어그램 | 7개 |
| **합계** | **약 3,600줄** |

퀴즈 Bloom 분포: Understand 12 · Analyze 10 · Evaluate 8 · Remember 6 · Apply 6
(기초 코스이므로 암기 비중을 14 %로 낮췄다)

## Success Criteria 대조

- [x] 1. `dram_ddr/01` §5.1 셀 물리와 **중복 0** — charge sharing·latch·정귀환을 한 문단도 쓰지 않고 `:::note` 두 곳에서 링크
- [x] 2. `hbm/01`과 02장이 다른 착지점 — 저기는 제품 비교(DDR5/GDDR6/HBM3E 표), 여기는 설계 선택지 소진(네 손잡이)
- [x] 3. TSV·µbump·인터포저·전체 경로·열 — **5개 신규 주제**가 실제로 새 내용 (기존 토픽에 용어집 정의뿐이었음을 조사로 확인)
- [x] 4. 7장 전부 d2 최소 1개, 라벨 겹침 0 (5,132쌍 검사)
- [x] 5. 용어집 + 퀴즈 7편 구비
- [x] 6. `CI=true astro build` 682 pages Complete, 상대링크 329건 깨짐 0
- [x] 7. 사다리 4단이 전 토픽 index에서 일관되게 서술
- [x] 8. `$display`/`$finish`/`$stop` 0건

## 계획 대비 편차

**편차 1 — d2 예약어 결함 6건을 함께 수정 (계획에 없던 작업)**
04장 컨테이너가 붕괴한 원인을 파다가, d2 가 `top`/`left`/`near`/`label`/`class`/`link` 를 노드 키로 쓰면 **조용히 깨진다**는 것을 발견했다(exit 0, 경고 없음). 리프 노드면 라벨이 사라지고 키워드가 소문자로 표시되며, 컨테이너면 자식이 통째로 없어진다. 기존 6개 파일에 이미 그 결함이 있었고(`hbm_dv/06`은 5줄 라벨이 `top` 한 단어로 표시되고 있었다), 사용자 승인(A) 후 전부 수정했다. 재발 방지로 `gen_d2.mjs`에 검출기를 추가(exit 1)하고 README 에 규칙 0 을 넣었다.

**편차 2 — README d2 규칙 3항목 추가**
작업 중 실측으로 알아낸 것들이다.
- 규칙 0 — 예약어를 노드 키로 쓰지 않는다
- 규칙 1-b — **컨테이너 제목은 짧게**. 제목이 컨테이너 폭을 채우면 진입 화살표가 관통한다(01·05장에서 두 번 겪음)
- 규칙 1-c — **컨테이너 안의 `direction` 은 elk 가 무시한다.** 배치가 의미를 왜곡하면 노드 구성 자체를 바꾼다

**편차 3 — Q2-B 에 따라 장 구성 변경**
01장이 DRAM 기초를 통째로 차지하므로, 원래의 01(메모리는 왜 느린가)과 02(대역폭 두 갈래)를 **02장으로 합쳤다.** 7장은 유지.

**편차 4 — 기존 `hbm` 본문의 기초 링크 공백까지 수정**
계획 S12 는 사다리 표 갱신만 잡았으나, 조사 단계에서 발견한 문제(`hbm` 본문 03·05·06 에 기초 링크가 0건)를 함께 메웠다. 단순 링크가 아니라 **역할 경계**를 함께 밝혔다.

## 중복 회피가 실제로 어떻게 작동했는가

Q2-B 를 택해 01장을 독립 장으로 쓰면서도, `dram_ddr/01` §5.1 과의 중복을 다음으로 눌렀다.

| | `hbm_basics/01` | `dram_ddr/01` §5.1 |
|---|---|---|
| 서술 축 | 동작의 **흐름** (열고·읽고·닫고·되살린다) | 동작의 **물리** (charge sharing → latch → full rail) |
| 물리 심층 | **한 문단도 쓰지 않고 링크** | 정본 |
| 착지 | "그래서 타이밍이 생긴다" → 02장 | "그래서 tRCD·tRAS 가 존재한다" |

`index.mdx` 에 두 코스의 관계표를 명시해 독자가 어디로 가야 할지 알 수 있게 했다.

## 이 코스를 관통하는 교정 셋

각 장에서 독립적으로 나왔으나 결과적으로 코스의 축이 되었다. `quiz/index.md` 에 미리 배치했다.

1. **"HBM은 빠른 메모리다"** → 핀당 전송률은 GDDR 보다 **낮다**(4.8~8.0 vs 20 Gb/s). 빠른 게 아니라 **넓다**
2. **"쌓으면 대역폭이 올라간다"** → 쌓기가 늘리는 것은 **용량**이다. 근거: HBM4 는 스택 높이와 무관하게 채널 32개 고정 → 버스 폭 2,048비트 고정 → 대역폭 고정
3. **"거리를 줄였으니 지연도 줄었다"** → 전파 지연은 **처음부터 병목이 아니었다**(인터포저 0.03 ns / 전체 수십 ns). 얻은 것은 **에너지와 폭**

## 미이행

없음. 다만 이전 세션의 미해결 항목이 그대로 남아 있다 — `20260831_edu-content_RULE_PATCH.md` 의 `## Correcting Published Content` 블록을 `JH_CC/.claude/rules/edu-content.md`(정본)에 이식해야 한다. 이 머신에 `JH_CC` 가 없어 `~/.claude/` 에만 적용된 상태이며, 동기화가 단방향이라 다음 `sync_to_global.sh` 실행 시 사라진다.
