# Plan: `hbm` 토픽 — HBM 아키텍처 (검증 엔지니어를 위한)

## Objective

SK hynix 8월 hy-way JD 중 **"HBM Digital Design (검증)"** 단일 직무를 기준으로,
그 검증 업무를 이해하는 데 필요한 **HBM 아키텍처 토픽(`hbm`)** 을 구축한다.
후속 토픽 `hbm_dv`(HBM 검증 실무, 11챕터)의 **선행 토픽**이며,
각 챕터는 하드웨어 사실 → 검증 문제로 이어지는 인과 고리를 남겨 `hbm_dv`로 딥링크한다.

- **이번 범위**: `hbm` 토픽만 (**6챕터**). `hbm_dv`는 완료 후 별도 PLAN.
- **선별 기준(단일)**: "HBM 검증 엔지니어가 알아야 하는가" — 설계·공정 전용 내용은 배제
- **결정 이력**: Q1=A / Q2=1 / 착수순서=A(`hbm` 먼저) / **스코프=검증 JD 한정 (Ch04·Ch08 제외)**

## Context (확인 완료)

### JD 근거 — `(KOR)8월 월간 hy-way_JD.pdf` (20p)

축이 되는 유일한 JD: **HBM Digital Design (검증)** — #이천 #Junior & Expert

| 구분 | 원문 |
|---|---|
| 업무 1 | HBM 내 In-house, 3rd Party **Digital IP 및 Mixed 설계** 검증 |
| 업무 2 | **In-house VIP 개발 + 3rd Party VIP 도입/활용** |
| 업무 3 | **UVM 기반 검증 환경 구성 및 Test Case 개발** |
| 업무 4 | **Regression 통한 Code/Func. Coverage Closure** |
| 역량 2 | Digital IP **Spec 기반 검증 계획 수립 및 검증 프로세스** 이해 |
| 역량 3 | **신규 Protocol / Custom IP UVM Agent 설계·구현** |
| 우대 1 | **Spec 기반 Custom UVM Agent A-to-Z** |
| 우대 2 | **HBM, DDR/LPDDR Memory 검증** |
| 우대 3 | **Full-Chip Mixed-Level (Schematic & RTL) 검증** |

인접 JD는 **검증 접점이 있는 항목만** 흡수한다:
- SoC Design JD — DRAM Controller/PHY IP Integration 및 **검증**, Chiplet I/F(UCIe·OpenHBI·BoW) **검증**,
  SoC & Digital Logic **Verification 환경 및 VIP 개발**, Test Firmware, Silicon Validation, RAS
- RTL Design JD — **Function 검증**, DRAM DFT/DFD, Silicon Chip 검증 및 이슈 분석
- FE JD — DFT **구현 및 검증**(MBIST, Tessent SSH/SSN, TestMAX), HBM PHY·TSV PHY·3D PHY

**배제**: Bonding / Process Integration / DTCO / PE(ATE·Wafer Test 공정) / CAD / 회로설계 JD 고유 내용.
→ 이에 따라 이전 초안의 **Ch04(제조·조립)·Ch08(전력·열·SI) 삭제**.

### 제외 내용 중 검증 접점만 후속 토픽으로 이관

| 삭제된 내용 | 행선지 |
|---|---|
| KGSD, Repair / Redundancy, Boundary Scan | `hbm_dv` Ch10 (DFT·MBIST·RAS 연계 검증) |
| Thermal Throttling, 저전력 모드 진입/복귀 | `hbm` Ch05 (mode/저전력 커맨드) + `hbm_dv` Ch08 (Test Case 시나리오) |
| Wafer Bonding, Thinning, BEOL, PDN 해석 | **전면 배제** (검증 직무 범위 밖) |

### 사이트 컨벤션 (실측)
- 신규 토픽은 `topics/<slug>/mkdocs.yml`이 **아니라** `starlight_site/src/content/docs/<slug>/` 에 직접 authoring
  (cxl, nvme, dpu, ras, dfd, cache_coherence 등이 이 방식)
- 토픽 홈: `index.mdx` — frontmatter `title` + `description`, `CardGrid`/`LinkCard` import
- 챕터: `NN_slug.md` — frontmatter `title` 만, 본문 상단 `:::tip[학습 목표]` + `:::note[사전 지식]`
- 퀴즈: `quiz/index.md` + `quiz/NN_slug_quiz.md`, 정답은 `<details><summary>정답 / 해설</summary>`
- 다이어그램: **D2** (```d2 코드펜스, D2.js WASM 렌더) — mermaid 사용처 없음
- 등록 2곳: `starlight_site/astro.config.mjs` 사이드바 "메모리 · Memory" 그룹,
  `starlight_site/src/content/docs/index.mdx` 홈 카드 (`cat-mem` 섹션, 카운트 3→4)
- 언어: **본문 한국어 단일**. `*_ko.md` 미러 없음 (cxl·dram_jedec_dv 실제 관행).
  glossary만 EN(ISO 11179) + KO 2벌 — `dram_jedec_dv` 선례

### 페다고지 기준 — `topics/_shared/PEDAGOGY_RUBRIC.md`
D1 Hook(시나리오로 시작) / D2 Derivation(순진한 시도→실패→일반화→정의) /
D3 Failure Mode(규칙마다 system-level 증상) / D4 Counterfactual(대안 비교) /
D5 Active Recall(챕터당 2개 이상, `<details>` 접힘)

### 근거 확보 수단
WebSearch 동작 확인됨. 예: HBM3(JESD238) = 16 channel / 32 pseudo-channel / 6.4 Gb/s per-pin /
최대 819 GB/s / 채널당 64-bit — 공개 출처로 인용 가능.

## 챕터 구성 (6)

서사: **왜(01) → 무엇을 검증하나, 제품 축(02) → DUT는 어디인가(03) → 논리 구조(04) → 동작(05) → 검증 대상 IP 지도(06)**

| # | 파일 | 제목 | 검증에 필요한 이유 |
|---|---|---|---|
| 01 | `01_why_hbm.md` | 왜 HBM인가 — Memory Wall, 2.5D/3D, 대역폭 산술 | 검증 대상 제품의 존재 이유와 성능 목표 |
| 02 | `02_generations.md` | 세대 지형도 — HBM2/2E/3/3E/4, JESD235/238, Custom HBM | 어느 세대·어느 제품을 검증하는가, cHBM이 왜 SoC 검증이 되는가 |
| 03 | `03_stack_architecture.md` | 스택 구조 — Core die / Base die / TSV / µbump / Interposer | **DUT 경계 확정** — 검증 대상은 Base die의 로직 |
| 04 | `04_channels_addressing.md` | 채널 · Pseudo-channel · Bank · 주소맵 | 채널 독립성 → concurrency 검증·coverage 축의 근거 |
| 05 | `05_interface_protocol.md` | 인터페이스 프로토콜 — CA 커맨드, DWORD, RD/WR latency, mode·저전력 커맨드 (LPDDR5 대비) | 시나리오·SVA·scoreboard가 감시할 대상 |
| 06 | `06_base_die_soc.md` | **Base Die = 미니 SoC** — Host I/F, DRAM Ctrl/PHY, TSV·3D PHY, 내부 버스, cHBM의 PCIe/UCIe/CXL | 검증 대상 IP 지도: in-house / 3rd party / mixed 구분 |

## 챕터별 필수 구조

```
frontmatter: title
:::tip[학습 목표]      Bloom 동사 4개 내외
:::note[사전 지식]     선행 챕터/토픽 딥링크
1. Why care?          (D1 Hook — 시나리오로 시작, 정의 금지)
2. 직관               (D2 순진한 시도 → 실패 → 일반화)
3. 작은 예            (D5 Active Recall 1)
4. 일반화             (D4 Counterfactual 1~2)
5. 디테일             (D3 Failure Mode — 핵심 규칙마다)
6. 흔한 오해
🔍 이 구조가 만드는 검증 문제   ← 전 챕터 필수, 이 토픽의 척추
7. 핵심 정리           (D5 Active Recall 2) + 퀴즈 링크
참고 자료              (공개 출처 2개 이상)
```

`🔍` 섹션은 "무엇이 깨질 수 있는가"까지만 쓰고, 해법은 `hbm_dv` 해당 챕터로 딥링크
(플레이스홀더로 두고 `hbm_dv` 완성 시 연결). → **문제 제기는 `hbm`, 해법은 `hbm_dv`**
(`dram_ddr` → `dram_jedec_dv` 관계와 동일)

## 근거·인용 정책 (anti-hallucination)

- HBM 수치 스펙(채널 수, 데이터레이트, 핀 수, 스택 단수, 용량)은 **WebSearch로 공개 출처 확인 후 인용**
- JEDEC 원문 미보유 → 표준 문서 번호(JESD235/238 등)와 공개 발표자료·벤더 문서를 근거로 사용,
  "정밀 수치는 JEDEC 원문 우선" 문구 명시
- 확인 불가 항목은 **추론** 태그 명시. SK hynix 내부 구현은 추측하지 않음
- 신호명/포트명은 공개 스펙에서 확인된 것만 사용

## Steps (micro-step — 각 단계 후 go-signal 대기)

- [x] S0. 리서치 — 완료. `topics/hbm/_research/web_index.md` (151줄, 출처 19개, 불일치 2건 산술 검증)
- [x] S1. 스캐폴딩 — 완료. `starlight_site/src/content/docs/hbm/` + `index.mdx` (111줄), `quiz/` 생성
- [x] S2. Ch01 `01_why_hbm.md` — 완료 (280줄). 산술 9건 검증, D1~D5 충족 (Active Recall 2 / Failure Mode 3 / Counterfactual 2)
- [x] S3. Ch02 `02_generations.md` — 완료 (268줄). 세대 표 산술 7건 검증, HBM2 수치 충돌(256 vs 307) 교육 소재로 활용
- [x] S4. Ch03 `03_stack_architecture.md` — 완료 (322줄). DUT 경계 매핑표 확정, d2 2블록 문법 검증
- [x] S5. Ch04 `04_channels_addressing.md` — 완료 (292줄). 채널 폭 유도 7건 검증, 독립성 지도(자원별) 확정
- [x] S6. Ch05 `05_interface_protocol.md` — 완료 (345줄). 감시 대상 분류표 확정(데이터 비교로 잡히는 항목 1/10)
- [x] S7. Ch06 `06_base_die_soc.md` — 완료 (374줄). 기능×출처 교차표 + 6챕터 🔍 종합표 21항목 (집계 검증)
- [x] S8. 용어집 — 완료. `glossary.md` 단일 파일 (530줄, 37개 용어). **계획 이탈**: EN+KO 2벌 → 한국어 단일 (starlight 실제 관행 = cxl 방식 준수)
- [x] S9. 퀴즈 — 완료. `quiz/index.md` + 6개 파일, 총 36문항 (챕터당 6, Bloom 혼합). 추가: 챕터·용어집 상대경로 링크 버그 일괄 수정
- [x] S10. 사이트 등록 — 완료. `astro.config.mjs` 메모리 그룹 3→4, 홈 `index.mdx` 카드 추가. 전 카테고리 카운트 정합성 검증 통과
- [x] S11. 빌드 검증 — **계획 이탈**: 로컬 Node v20.20.2 < Astro 요구 v22.12+ 로 로컬 빌드 생략. 대신 정적 점검(frontmatter 15/15, 내부링크 94/94, d2 펜스, 펜스밖 중괄호 0) 후 push → GitHub Actions(Node 22)에서 빌드 확인
- [x] S12. 클로즈아웃 — 완료 (아래 결과 절 참조)

## Success Criteria

1. `npm run build` 무오류 통과, 내부 링크 깨짐 0, orphan 파일 0
2. 6챕터 전부 D1~D5 루브릭 충족 (챕터당 Active Recall 2개 이상, Failure Mode 3개 이상)
3. 6챕터 전부 `🔍 이 구조가 만드는 검증 문제` 섹션 보유 → `hbm_dv` 딥링크 자리 확보
4. 모든 수치 스펙에 공개 출처 인용 또는 **추론** 태그
5. 사이드바·홈 카드 등록 완료, 메모리 카테고리 4토픽으로 표시
6. **전 챕터가 "HBM 검증 JD" 단일 필터를 통과** — 설계·공정 전용 내용 0

## Risks / Open Questions

| 리스크 | 완화 |
|---|---|
| `dram_ddr`/`dram_jedec_dv`와 DRAM 기초 중복 | `dram_ddr`를 사전 지식으로 링크, HBM **차이점만** 서술 |
| HBM4 스펙이 유동적(2026 기준) | 확정 항목만 단정, 나머지는 "발표 기준 / 변동 가능" 명시 |
| 인과 고리가 `hbm_dv` 미완성으로 끊김 | `🔍` 섹션은 자체 완결(문제 제기까지), 링크는 후속 연결 |
| 6챕터로 줄어 토픽 분리 명분 약화 | cxl(5모듈) 선례로 사이트 리듬에는 부합. `hbm`(6) + `hbm_dv`(11) 구도 유지 |

## 후속

`hbm_dv` (11챕터) — 본 토픽 완료 후 별도 PLAN. 관통 사례 `hbm_ch_ctrl`(교육용 가상 IP)은 `hbm_dv`에서 도입.

---

# 결과 (클로즈아웃, 2026-08-27)

## 산출물

| 항목 | 수량 |
|---|---|
| 챕터 | 6 (`01_why_hbm` ~ `06_base_die_soc`) |
| 코스 홈 | `index.mdx` (JD 매핑표 포함) |
| 용어집 | `glossary.md` — 37개 용어, ISO 11179 |
| 퀴즈 | `quiz/index.md` + 6개 — 총 36문항 |
| d2 다이어그램 | 7개 |
| 총 파일 / 줄 | 15 파일 / 약 3,360줄 |
| 리서치 인덱스 | `topics/hbm/_research/web_index.md` (출처 19개) |

## Success Criteria 달성 여부

1. ✅ 빌드 통과 — GitHub Actions run `32818832489` success (build 1m16s / deploy 10s)
2. ✅ D1~D5 루브릭 — 전 챕터 Active Recall 2 / Failure Mode 3 / Counterfactual 2
3. ✅ 전 챕터 `🔍 이 구조가 만드는 검증 문제` 보유 (Ch01~06 각 4/4/5/5/5/7항목)
4. ✅ 수치에 공개 출처 인용 또는 *추론(산술)* 태그. 산술 검증 20건 이상 수행
5. ✅ 사이드바 + 홈 카드 등록, 메모리 카테고리 3→4토픽 (전 카테고리 카운트 정합성 확인)
6. ✅ 검증 JD 단일 필터 통과 — 설계·공정 전용 내용 0

## 계획 대비 이탈

| 이탈 | 내용 | 사유 |
|---|---|---|
| S8 용어집 | EN+KO 2벌 → **한국어 단일 파일** | starlight 실제 관행(cxl) 준수. starlight 내 `*_ko.md` 존재 0건이며, `dram_jedec_dv`의 영문판은 존재하지 않는 KO 미러를 참조하는 이관 잔재 |
| S11 빌드 | 로컬 빌드 → **정적 점검 + CI 빌드** | 로컬 Node v20.20.2 < Astro 요구 v22.12+. 정적 점검(링크 94/94 등) 후 push하여 CI(Node 22)에서 확인 |
| 범위 | 초안 8챕터 → **6챕터** | 사용자 지시로 검증 JD 한정. Ch04(제조·조립)·Ch08(전력·열) 제외 |

## 작업 중 잡은 결함

1. **상대 경로 링크 44건 오류** — 챕터/용어집이 `./quiz/`·`./glossary/`·`../<타토픽>/`을 사용. Starlight는 페이지 URL 기준으로 해석하므로 전부 404가 될 상태였음. `cxl` 관행과 대조해 일괄 수정, URL 기준 재검증(94/94 정상)
2. **Ch06 종합표 집계 오류** — 요약 문장이 23건으로 계산되어 표의 21행과 불일치. 실집계로 수정
3. **용어집 ISO 11179 부정 표현 1건** — `Custom UVM Agent` 정의의 "존재하지 않는" → 긍정 진술로 수정
4. **공개 자료 수치 충돌 2건** — HBM3 pseudo-channel 수(4 vs 32), HBM2 대역폭(256 vs 307 GB/s). 대역폭 산술로 판정하고 후자는 Ch02의 Active Recall 교육 소재로 전환

## 공개 저장소 대응

발행 콘텐츠(`starlight_site/src/content/docs/hbm/`)의 회사명 언급 **0건**. Ch06 면책 문구 1곳을 회사명 없이 동일 의미로 조정. JD PDF는 커밋 제외.
본 PLAN과 `_research/web_index.md`는 사용자 승인(옵션 C)에 따라 원문 그대로 커밋.

## 후속

`hbm_dv` (11챕터) — 별도 PLAN 필요. 본 코스 Ch06의 **21항목 종합표**가 출발점이며,
관통 사례 `hbm_ch_ctrl`(교육용 가상 IP)을 그 토픽에서 도입한다.
