# RDMA for NRT

<!-- id=1047199830 parent=7012616 -->

**RDMA for CS**

- QP 512 per way

- MR 1024 per way

- SRQ 256 per way

- OPS

- PCC

- ACP (write allocation on ARM L2 cache)

- num_sge = 6 for send WQEs

- MMU-based memory management

**RDMA for CS NRT**

- QP 32 per way

- MR ? per way

- SRQ ? per way

- no OPS

- no PCC?

- no ACP (write allocation on ARM L2 cache)?

- num_sge = 2 for send WQEs

- MTT cache-based memory management

==

**CS timeline for RDMA**

RDMA v1 (resource opt) → datapath optimization (read perf opt with bugfix) → SRQ 256 per way (with resource opt) → num_sge = 6 for send WQE → MMU → ACP

**CS timeline for RDMA** **for NRT**

RDMA v1 (resource opt) → datapath optimization (read perf opt with bugfix) → SRQ ? per way (with resource opt)

==

\(1\) 검증 ownership

\(2\) RDMA for NRT 개발 ownership

\(3\) 디자인 분기 해결 방법?
