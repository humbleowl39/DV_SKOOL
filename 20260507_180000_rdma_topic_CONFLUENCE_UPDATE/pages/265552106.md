# ECE (Enhanced Connection Establishment)

<!-- id=265552106 parent=32080200 -->

This was introduced in IBTA 1.7

NCCL environment variables

- NCCL_IB_GID_INDEX

  - 3 \<\< is_roce 결정

- NCCL_NET_GDR_READ

  - <div>

    <div>

    Env. variable & Options

    </div>

    </div>

  - PCIe에서는 기본적으로 0 → sender 쪽에서는 GDR 없고, receiver 쪽에서만 있음

- NCCL_P2P_LEVEL

  - 0 (LOC) or 4 (SYS)

    - LOC: Never use

    - PIX / PXB / PHB

    - SYS: Use P2P between NUMA nodes, potentially crossing the SMP interconnect

- NCCL_GDR_FLUSH_DISABLE

- NCCL_NET_GDR_LEVEL

  - 0 or 4 (0이면 off, 4면 on)

- NCCL_BUFFSIZE

  - 모든 collective communication 관련 MR의 크기를 고정시킴 (e.g., 512KB)

    - Default 4MB → circular fifo, each chunk = 512KB → cache size 등을 고려한 크기?

    - <a href="https://github.com/NVIDIA/nccl/issues/544" data-card-appearance="inline">https://github.com/NVIDIA/nccl/issues/544</a>

  - 다만 point-to-point communication (e.g., send-recv)의 경우에는 remote peer의 개수에 따라 memory 소모량을 늘리는 대신, 하나의 거대한 공유 buffer를 잡아놓고 모든 remote peer 와의 통신에 사용함 (e.g., 8MB MR)

    - NCCL_NET_SHARED_BUFFERS를 0으로 만들면 공유 buffer를 사용하지 않게 할 수 있음

      - RCCL은 이 옵션을 제대로 지원하지 못하는 듯

    - 공유 buffer의 크기는 (# of channels) \* 16 \* NCCL_P2P_NET_CHUNKSIZE

      - \# of channel은 scale에 dependent 할 수 있음

        - 대략 상한이 2 \* NICs/node 임 → 이 경우 32MB MR

- NCCL_NCHANNELS

  - MAX 와 MIN이 있음

  - Determine 되는 것은 크게 3가지 (1) PCIe bw, (2) RNIC이 hold할 수 있는 connection 수, (3) gid

  - Channel의 경우 SM에 mapping 됨

- NCCL_P2P_NET_CHUNKSIZE

  - Default 128KB → large data transfer가 이 단위로 쪼개짐

  - 그래서 128KB가 제일 많은 것인가

- NCCL_IB_MERGE_NICS

  - Enable NCCL to combine dual-port IB NICs into a single logical network device. This allows NCCL to more easily aggregate dual-port NIC bandwidth.

  - 2x200Gbps 를 1x400Gbps에 비비게 해줌?

- NCCL_PXN_DISABLE

  - Cross-rail traffic을 막음

- NCCL_GDRCOPY_ENABLE

  - Deprecated

- NCCL_GDRCOPY_FLUSH_ENABLE

  - Deprecated

  - <a href="https://github.com/NVIDIA/nccl/issues/683" data-card-appearance="inline">https://github.com/NVIDIA/nccl/issues/683</a>

  - <a href="https://github.com/NVIDIA/nccl/issues/475" data-card-appearance="inline">https://github.com/NVIDIA/nccl/issues/475</a>

  - Memory barrier through loopback read

- NCCL_NET_GDR_READ

  - The NCCL_NET_GDR_READ variable enables GPU Direct RDMA when sending data as long as the GPU-NIC distance is within the distance specified by NCCL_NET_GDR_LEVEL. Before 2.4.2, GDR read is disabled by default, i.e. when sending data, the data is first stored in CPU memory, then goes to the InfiniBand card. Since 2.4.2, GDR read is enabled by default for NVLink-based platforms.

    Note: Reading directly from GPU memory when sending data is known to be slightly slower than reading from CPU memory on some platforms, such as PCI-E.

더 알아야 되는 variable 들

- NCCL_IB_QPS_PER_CONNECTION

  - QP에 data를 interleaving (정확히는 message) → Entropy 증가

- NCCL_IB_SPLIT_DATA_ON_QPS

  - Each message is split into QPs → may cause visible latency degradation

- NCCL_IB_ADAPTIVE_ROUTING

  - Enabled in IB, Disabled in RoCE by default

  - NCCL_IB_SL 도 설정해주어야함?

- NCCL_IB_ECE_ENABLE

  - Enable the use of Enhanced Connection Establishment (ECE) on IB/RoCE Verbs networks. ECE can be used to enable advanced networking features such as Congestion Control, Adaptive Routing and Selective Repeat. Note: These parameters are not interpreted or controlled by NCCL and are passed through directly to the HCAs via the ECE mechanism.

- NCCL_NET_SHARED_COMMS

  - Reuse the same connections in the context of PXN. This allows for message aggregation but can also decrease the entropy of network packets.

- NCCL_IB_MERGE_NICS

  - Enable NCCL to combine dual-port IB NICs into a single logical network device. This allows NCCL to more easily aggregate dual-port NIC bandwidth.

===  
<a href="https://chatgpt.com/share/6804a5b9-9034-8001-ae72-c5fa9c5d0bcf" data-card-appearance="inline">https://chatgpt.com/share/6804a5b9-9034-8001-ae72-c5fa9c5d0bcf</a>

<a href="https://patchwork.kernel.org/project/linux-rdma/patch/20200413141538.935574-6-leon@kernel.org/" data-card-appearance="inline">https://patchwork.kernel.org/project/linux-rdma/patch/20200413141538.935574-6-leon@kernel.org/</a>

ECE를 하기 위해서, CM에 새로운 field를 추가

- Vendor ID field (24bit)

  - e.g., Mellanox vendor ID

- AttributeModifier (32bit) \<\< ?

  - 각각의 bit는 특정 feature에 대한 enabled 여부를 의미 → vendor 마다 다를 수 있음

CM을 하면, vendor id를 보고 attribute modifier를 해석한다. → Vendor 가 다르거나 하면 CM REJ 메세지로 알려줄 수 있음 (IB_CM_REJ_VENDOR_OPTION_NOT_SUPPORTED)

===

- PCC를 하기 위해서 어떤 event 들이 필요한가? Multi-path aware? (source ID?)

  - 위 ECE에 대한 내용 파악

  - Xilinx Versal HPS 로 성능이 충분한가?

    - 단순 생각: (1) “Active”한 QP 개수가 많지 않다면 지금 core로도 충분 (2) DPA는 “여러 QP”가 있을 때 필요한 것?

      - 실제 RCCL 돌려보면서 test 가능?

  - Timing packet 을 지원하기 위한 SW 적 수정?

- 우리가 Packet spray를 하기 위해서는 “SW” 적으로 어떤 support가 필요한가?

  - 위 NCCL 옵션을 그대로 쓸 수 있는가? (e.g., NCCL_IB_ADAPTIVE_ROUTING)

    - RDMA 4K Write → 4x RDMA write only

    - RDMA 4K Write with imm → 4x RDMA write only and zero-length write-with-imm

  - RCCL 은 위 옵션들을 지원하는가?

  - Switch config를 어떻게 해야되는가? (ECMP?)

    - 간단한 testbed 세팅 가능?

- NS-3 알아야 되는가?
