# Plan: `hbm_dv` 토픽 — HBM 검증 실무

## Objective

선행 토픽 `hbm`(HBM 아키텍처, 6챕터, 배포 완료)이 제기한 **검증 문제 21항목**을
실제 UVM 검증 환경으로 옮기는 과정을 다루는 토픽을 구축한다.

- **역할 분담**: 문제 제기는 `hbm`, **해법은 `hbm_dv`**
- **기준 JD**: HBM Digital Design (검증) — 단일 직무 (선행 토픽과 동일 스코프)
- **관통 사례**: 교육용 가상 IP `hbm_ch_ctrl` — spec을 직접 작성하고 12챕터 내내 확장

## Context — 선행 작업에서 확정된 것

### `hbm` 토픽 (완료, 커밋 `ab5c5c4` / `461929e`, 배포 성공)
- 6챕터 + 용어집 37 + 퀴즈 36문항, https://humbleowl39.github.io/DV_SKOOL/hbm/
- **Ch06 종합표 21항목** — 본 토픽의 요구사항 명세서 역할
- 확정된 개념: DUT 경계(RTL로 표현되는 영역) / 기능×출처 교차표 /
  네 번의 "조용한 통과"(관대한 모델·주소 디코드·프로토콜 위반·ECC 마스킹)

### 사이트 컨벤션 (실측·검증 완료)
- 위치: `starlight_site/src/content/docs/<slug>/`, 챕터 `NN_slug.md`, 홈 `index.mdx`
- **상대 경로 규칙 (중요 — `hbm`에서 44건 오류를 잡았던 지점)**
  - 챕터 → 챕터: `../NN_slug/`
  - 챕터 → 퀴즈/용어집: `../quiz/xxx/`, `../glossary/`
  - 챕터 → 타 토픽: `../../<topic>/`
  - 코스 홈(`index.mdx`) → 챕터: `./NN_slug/` / 타 토픽: `../<topic>/`
  - 퀴즈 본문 → 챕터: `../../NN_slug/`
- 다이어그램 **d2** (```d2 펜스), admonition `:::tip/:::note/:::caution`, 접힘 `<details>`
- 용어집: **한국어 단일 파일** + 영문 표제어, ISO 11179 5필드
- 퀴즈: 챕터당 6문항, Bloom 혼합, `<details>` 정답
- 등록 2곳: `astro.config.mjs` 사이드바 + 홈 `index.mdx` 카드(카운트 동기화)
- **빌드**: 로컬 Node v20은 미달. `export PATH="$HOME/.nvm/versions/node/v22.22.3/bin:$PATH"` 후 `npm run build`

### 페다고지 — `topics/_shared/PEDAGOGY_RUBRIC.md`
D1 Hook / D2 Derivation / D3 Failure Mode / D4 Counterfactual / D5 Active Recall.
`hbm`에서 확립한 챕터 골격을 유지하되 **🔍 절을 🔧 절로 전환**(아래 참조).

## 선행 토픽과의 구조적 차이 3가지

| 항목 | `hbm` | `hbm_dv` |
|---|---|---|
| 고정 섹션 | **🔍 이 구조가 만드는 검증 문제** (문제 제기) | **🔧 이 문제를 이렇게 푼다** (해법 + `hbm` 21항목 중 어느 것을 닫는지 명시) |
| 코드 | 없음 | **SystemVerilog / UVM 코드 포함** |
| 관통 사례 | 없음 | **`hbm_ch_ctrl`** 이 Ch01→Ch12 전체를 관통 |

## 중복 회피 — 기존 토픽 경계 (실측)

| 토픽 | 이미 다루는 것 | `hbm_dv`의 처리 |
|---|---|---|
| `uvm` (8모듈) | phase, agent/driver/monitor, sequence, config_db/factory, TLM/scoreboard/coverage, 실무 패턴, RAL | **기초는 링크만.** HBM 고유 적용에만 집중 |
| `mixed_signal_dv` Ch12 | **UVM × RNM Integration (Env·Agent·Sequence·Scoreboard)** | ⚠️ 정면 중복 위험. Ch05는 **RNM이 아니라 full-chip mixed-level(Schematic & RTL)** 에 한정하고 RNM 방법론은 링크 |
| `ras` Ch03 | **RAS-node & Fault Injection (DV)** | ⚠️ 중복 위험. Ch11은 **HBM 고유**(on-die ECC 마스킹, MBIST-MPPR, IEEE 1500 TAP)에 한정 |
| `dram_jedec_dv` Ch10/11 | DV Methodology, DV 프로젝트 End-to-End | Ch12 캡스톤이 DRAM 일반론을 반복하지 않도록 **base die/SoC 관점**으로 차별화 |

## 챕터 구성 (12) — 합의된 11챕터에서 1개 신설

> **변경 제안**: 합의안은 11챕터였으나 **Ch09 Assertion·Protocol Checker를 신설**하여 12챕터로 조정.
> 사유: 21항목 중 **Assertion 성격이 3건**(#3 커맨드 타이밍/순서, #13 CA 경합·중재, #14 row-col 순서)이고
> Scoreboard 항목(#15)까지 더하면 4건인데, 기존 11챕터 어디에도 이들을 담을 자연스러운 자리가 없었다.
> 선행 토픽 Ch05가 *"감시 대상 10개 중 데이터 비교로 잡히는 것은 1개"* 로 결론냈으므로,
> assertion을 곁다리로 두면 코스의 핵심 논지가 약해진다.

| # | 파일 | 제목 | 닫는 21항목 |
|---|---|---|---|
| 01 | `01_what_we_verify.md` | 무엇을 검증하는가 — DUT 경계·업무 사이클·`hbm_ch_ctrl` 소개 | #9 |
| 02 | `02_ip_verification_handoff.md` | Digital IP 검증 & IP Handoff (in-house / 3rd party) | #19, #21 |
| 03 | `03_custom_uvm_agent.md` | **Custom UVM Agent A-to-Z** — `hbm_ch_ctrl` spec → agent | #7 |
| 04 | `04_vip_strategy.md` | VIP 전략 — 사느냐 만드느냐, HBM memory model | #20, #10 |
| 05 | `05_mixed_level.md` | Full-Chip Mixed-Level 검증 (Schematic & RTL) | #11 |
| 06 | `06_env_hierarchy.md` | UVM 환경 계층 — IP → Subsystem → Full-chip, Test Firmware | #8, #12 |
| 07 | `07_vplan_process.md` | V-Plan 수립 & 검증 프로세스 | #4, #5 |
| 08 | `08_testcase_scenarios.md` | Test Case & 시나리오 라이브러리 (에러 주입 포함) | #2, #16, #17 |
| 09 | `09_assertion_checker.md` | **Assertion · Protocol Checker** *(신설)* | #3, #13, #14, #15 |
| 10 | `10_coverage_regression.md` | Coverage Closure & Regression 운영 | #1, #6, #18 |
| 11 | `11_dft_ras.md` | DFT · MBIST · RAS 연계 검증 | #10(ECC 관측), #18(모드 전이) |
| 12 | `12_end_to_end.md` | **End-to-End 캡스톤** — spec 1장 → sign-off → silicon 이슈 | 전체 관통 |

**부속**: 부록 A `appendix_a_hbm_ch_ctrl_spec.md` (관통 사례 spec) · 용어집 · 퀴즈 12종(72문항)

**21항목 전량 매핑 확인**: 1→10, 2→08, 3→09, 4→07, 5→07, 6→10, 7→03, 8→06, 9→01, 10→04·11,
11→05, 12→06, 13→09, 14→09, 15→09, 16→08, 17→08, 18→10·11, 19→02, 20→04, 21→02 — **누락 0**

## 관통 사례 `hbm_ch_ctrl` (교육용 가상 IP)

**왜 가상 IP인가**: 실제 제조사 내부 IP를 추측하지 않으면서 JD 우대사항
*"Spec 기반 Custom UVM Agent A-to-Z"* 를 그대로 재현하기 위함. spec을 우리가 작성하므로
"스펙에서 검증 항목을 도출하는" 과정 전체를 보여줄 수 있다.

**설계 방향** (부록 A에서 확정)
- Base die 내 채널 컨트롤러 + 비표준 side-band 제어 인터페이스
- 선행 토픽에서 확정한 구조적 성질을 의도적으로 포함:
  semi-independent pseudo-channel(CA 공유), 분리된 row/column 커맨드 흐름,
  mode register, 에러 검출/보고, 테스트 모드 진입·복귀
- **상용 VIP가 존재하지 않는 인터페이스**로 정의 → Ch03의 Custom Agent 개발이 필연이 되게 함
- 챕터별 확장: Ch03 agent → Ch08 시나리오 → Ch09 assertion → Ch10 coverage → Ch12 캡스톤

## 코드 작성 규칙 (신규 — `hbm` 토픽에 없던 항목)

- **금지**: `$display` / `$finish` / `$stop` → `uvm_info` / `uvm_error` / `uvm_fatal` 사용
- **금지**: 하드코딩 절대경로·IP 주소 → 플레이스홀더 또는 환경변수
- UVM **1.2** 기준. factory 등록·config_db 경로·phase 사용이 일관되어야 함
- 발췌가 아니라 **맥락이 이해되는 단위**로 제시 (필요한 선언 포함)
- 실제 컴파일 가능성을 목표로 하되, 시뮬레이터 실행 검증은 범위 밖임을 명시
- 선행 지식(phase/factory/config_db)은 설명하지 않고 [UVM 코스](../../uvm/)로 링크

## Steps (micro-step — 각 단계 후 go-signal 대기)

- [x] S0. 리서치 — 완료. `topics/hbm_dv/_research/web_index.md` (125줄, 출처 10개). 서사 영향 발견 2건(상용 HBM VIP 실재 / UVM 버전 정책) + 기존 토픽 링크 맵 확정
- [x] S1. 스캐폴딩 — 완료. `starlight_site/src/content/docs/hbm_dv/` + `index.mdx` (189줄), `quiz/` 생성. 21항목→챕터 매핑표 검증(21/21)
- [x] S2. 부록 A `appendix_a_hbm_ch_ctrl_spec.md` — 완료 (310줄). 규칙 R1~R20 연속성 검증, SV 인터페이스 포함, 검증 관점 인덱스
- [x] S3. Ch01 `01_what_we_verify.md` — 완료 (330줄). #9 닫음. 경계 문서 템플릿 + 세 판정 질문 + 업무 사이클
- [x] S4. Ch02 `02_ip_verification_handoff.md` — 완료 (348줄). #19·#21 닫음. Handoff 체크리스트 H1~H6 + 통합 검증 I1~I8
- [x] S5. Ch03 `03_custom_uvm_agent.md` — 완료 (674줄). #7 닫음. SV 블록 7개 균형 검증, 스펙 규칙 9종 인용 정합. 코드 금지패턴 0
- [x] S6. Ch04 `04_vip_strategy.md` — 완료 (367줄). #20·#10 닫음. 3단 절차 + 검사범위 매핑표 + VIP 평가 V1~V8
- [x] S7. Ch05 `05_mixed_level.md` — 완료 (339줄). #11 닫음. 구성 프로파일 P0/P1/P2 + 프로파일 독립성 규칙. RNM 방법론 미서술(링크만)
- [x] S8. Ch06 `06_env_hierarchy.md` — 완료 (478줄). #8·#12 닫음. 구성값 3분류(폭=컴파일/개수=런타임) + 계층 env 코드 + 자극 현실성 역방향 피드백
- [x] S9. Ch07 `07_vplan_process.md` — 완료 (414줄). #4·#5 닫음. V-Plan 행 5요소 + 앞 챕터 산출물 통합 + 성능 절 분리 + 세대이관 절차
- [x] S10. Ch08 `08_testcase_scenarios.md` — 완료 (534줄). #16·#17·#2 닫음. 3계층 시퀀스 라이브러리 + 오류주입/설정효과/성능 시나리오 코드
- [x] S11. Ch09 `09_assertion_checker.md` — 완료 (604줄). #3·#13·#14·#15 닫음. 규칙 3분류(SVA/Scoreboard/절차적) + vacuity 방지(cover 짝) + 독립 주소 디코드
- [x] S12. Ch10 `10_coverage_regression.md` — 완료 (503줄). #1·#6·#18 닫음. 동시성 축(상태 기반) + 전이 bin + 동적 bin + exclusion 정당화
- [x] S13. Ch11 `11_dft_ras.md` — 완료 (435줄). #10·#18 마무리. 관측 사각(ECC 마스킹) + 투명성 레지스터 + ECC×MBIST 대조군 + 스펙 빈틈 처리 4단계. ras 중복 0
- [x] S14. Ch12 `12_end_to_end.md` — 완료 (455줄). 캡스톤 — 7 Phase 관통 + 실패 3종 분류 + silicon postmortem. 21항목 매핑 검증(21/21)
- [x] S15. 용어집 — 완료. `glossary.md` (525줄, 37개 용어). ISO 11179 5필드 37/37, 다중문장·부정표현·순환정의 위반 0. 검증 실무 용어 중심(HBM 도메인은 선행 코스 용어집에 위임)
- [x] S16. 퀴즈 — 완료. `quiz/index.md` + 12개 파일, 총 72문항 (챕터당 6, Bloom 혼합). 추가: 챕터·부록 상대경로 링크 일괄 수정 (hbm 토픽과 동일 유형 버그 재발 → 전수 검증 228/228)
- [x] S17. 사이트 등록 — 완료. 사이드바 메모리 그룹 4→5 (`hbm` 바로 뒤에 `hbm_dv`), 홈 카드 추가. 전 카테고리 카운트 정합성 검증 통과
- [x] S18. 역방향 링크 — 완료. `hbm` 9곳 평문→링크화 + **Ch06 21항목 표에 "닫는 곳" 열 추가**(각 항목 → 해당 hbm_dv 챕터 딥링크 21/21). 양방향 링크: hbm→hbm_dv 31, hbm_dv→hbm 42. 전체 353/353 정상
- [x] S19. 검증 — 정적 점검 완료 (43파일). frontmatter 43/43, 링크 353/353, d2·펜스 균형, SV 금지패턴 0, details 짝 OK, 규칙 인용 정합, **신호명 드리프트 1건 발견→수정**(MR_*_ADDR 미정의 → 부록 A.7.1 추가, spec v1.1). 🔧 12/12, 🔍 6/6. **로컬 빌드는 사용자 선호에 따라 생략 — CI에서 확인**
- [x] S20. 클로즈아웃 — 완료 (아래 결과 절 참조)

## Success Criteria

1. `npm run build`(Node 22) 통과, 내부 링크 깨짐 0 — **URL 기준**으로 검증
2. 12챕터 전부 D1~D5 루브릭 충족 (Active Recall 2+ / Failure Mode 3+ / Counterfactual 1+)
3. 12챕터 전부 **🔧 절** 보유하며, `hbm` 21항목 중 담당 항목을 명시적으로 닫음 → **21/21 커버**
4. 모든 코드 예제가 금지 패턴(`$display`/`$finish`/`$stop`/하드코딩 경로) 0건
5. `hbm_ch_ctrl`이 Ch03·08·09·10·12에서 **일관되게** 확장됨 (신호명·필드명 불일치 0)
6. 기존 토픽(`uvm`/`mixed_signal_dv` Ch12/`ras` Ch03)과 내용 중복 없이 링크로 연결
7. `hbm` 토픽의 9개 `hbm_dv` 언급이 전부 실제 링크로 연결됨

## Risks / Open Questions

| 리스크 | 완화 |
|---|---|
| `mixed_signal_dv` Ch12와 정면 중복 (UVM×RNM) | Ch05를 **full-chip mixed-level(Schematic & RTL)** 로 한정, RNM은 링크 |
| `ras` Ch03과 fault injection 중복 | Ch11을 **HBM 고유**(ECC 마스킹·MBIST-MPPR·IEEE1500 TAP)로 한정 |
| `uvm` 토픽 재탕 | 모든 챕터에서 "UVM 기초는 링크, HBM 적용만 서술" 원칙 적용 |
| 코드 예제가 컴파일 불가 상태로 방치 | 금지 패턴 자동 점검 + 선언 누락 수동 리뷰. **시뮬레이터 실행 검증은 범위 밖임을 명시** |
| 가상 IP가 비현실적이면 학습 가치 하락 | 선행 토픽에서 근거 확보한 **실제 구조적 성질**(CA 공유·row/col 분리·mode reg·에러 보고)만으로 구성 |
| 12챕터 + 코드로 분량 과다, 세션 분할 필요 | micro-step 유지, `.claude/claude-progress.md`에 진행 기록 |
| 챕터 간 `hbm_ch_ctrl` 신호명 드리프트 | 부록 A를 **단일 진실 공급원**으로 두고 각 챕터가 인용. S19에서 이름 일치 자동 점검 |

## 공개 저장소 주의

- 발행 콘텐츠에 **회사명 미기재** (선행 토픽과 동일 원칙)
- 실제 제조사 내부 IP·신호명 추측 금지 — 관통 사례는 **교육용 가상 IP임을 매 챕터 명시**
- JD 인용은 공개 채용 문서 범위 내에서, 회사명 없이 "직무 기술서"로 지칭

---

# 결과 (클로즈아웃, 2026-08-28)

## 산출물

| 항목 | 수량 |
|---|---|
| 챕터 | 12 (`01_what_we_verify` ~ `12_end_to_end`) |
| 코스 홈 | `index.mdx` — 21항목→챕터 매핑표, JD 매핑, 코드 정책 |
| 부록 | A — `hbm_ch_ctrl` 스펙 (규칙 R1~R20, CSR 맵, 주소 상수) |
| 용어집 | 37개 용어, ISO 11179 (검증 실무 용어 중심) |
| 퀴즈 | 13파일 / **72문항** (챕터당 6, Bloom 혼합) |
| SystemVerilog 코드 블록 | 19개 |
| 총 파일 / 줄 | **28 파일 / 약 8,400줄** |
| 리서치 인덱스 | `topics/hbm_dv/_research/web_index.md` (출처 10개) |

## Success Criteria 달성 여부

1. ✅ 빌드 — GitHub Actions run `33130249432` **success** (build 56s / deploy 8s). 로컬 빌드는 사용자 선호로 생략
2. ✅ 12챕터 D1~D5 루브릭 — 전 챕터 Active Recall 2 / Failure Mode 3 / Counterfactual 2
3. ✅ **21/21 항목 전부 담당 챕터에서 닫음** — 🔧 절 12/12, 닫는 항목 명시
4. ✅ 코드 금지 패턴 0건 (`$display`/`$finish`/`$stop`), `uvm_*` 리포팅만 사용
5. ✅ `hbm_ch_ctrl` 일관성 — 신호명 드리프트 0, 스펙 정의 31개 이름 전부 사용
6. ✅ 기존 토픽 중복 회피 — RNM 방법론 0건(mixed_signal_dv 링크), ECC 원리 0건(ras 링크), UVM 기초는 링크만
7. ✅ 역방향 링크 — `hbm` 9곳 링크화 + **Ch06 21항목 표에 "닫는 곳" 열 21/21**

## 계획 대비 이탈

| 이탈 | 내용 | 사유 |
|---|---|---|
| 챕터 수 | 11 → **12** | Ch09(Assertion·Protocol Checker) 신설. 21항목 중 4건이 assertion 성격인데 담을 자리가 없었음 (계획 단계에서 제안·승인) |
| UVM 버전 | UVM 1.2 → **1.2 ∩ IEEE 1800.2 교집합** | 현행 표준은 1800.2이나 실무에 1.2가 널리 쓰임. 교집합이면 양쪽 독자가 그대로 사용 |
| S19 빌드 | 로컬 빌드 → **정적 점검 + CI** | 선행 토픽과 동일하게 사용자 선호 반영 |

## 작업 중 잡은 결함

1. **상대 경로 링크 137건 오류** — 챕터·부록이 `./quiz/`·`./NN_slug/`·`../hbm/` 사용. Starlight는 페이지 URL 기준으로 해석하므로 전부 404가 될 상태. **선행 토픽에서 같은 유형을 이미 겪었음에도 재발** → 일괄 수정 후 353/353 검증
2. **신호명 드리프트 1건** — Ch08 코드가 쓰는 `MR_*_ADDR` 상수가 부록 A에 미정의. A.7.1 추가, spec v1.1로 갱신
3. **서사 정정 1건** — S0 리서치에서 상용 HBM VIP 실재 확인. "Custom HBM이라 VIP가 없다"를 HBM 전체로 일반화하면 오류. Ch04에서 **표준 I/F는 구매가 기본값, 커스텀 I/F만 자체 개발**로 정밀화

## 코스가 만든 핵심 개념

- **여섯 번의 "조용한 통과"** — 관대한 모델(선행 Ch03) / 주소 디코드(선행 Ch04) / 프로토콜 위반(선행 Ch05) / monitor item 재사용(Ch03) / vacuous assertion(Ch09) / **ECC 마스킹(Ch11)**. 여섯 번 모두 원인이 같음: 관측 경로가 결함을 볼 수 없게 되어 있었던 것
- **"검증 기준을 검증 대상에서 가져오면 안 된다"** 의 세 형태 — Ch03(monitor↔driver item) / Ch09(assertion 기준값↔DUT CSR) / Ch11(테스트 기준↔RTL)
- **"좋은 검증은 모든 것을 잡는 것이 아니라 무엇을 잡지 않았는지 정확히 아는 것"** — 경계표·매핑표·프로파일 한계표·차단·cover·exclusion 정당화·스펙 빈틈 기록이 전부 "모르는 것"을 가시화하는 장치

## 공개 저장소 대응

발행 콘텐츠의 회사명 언급 0건. `hbm_ch_ctrl`은 **교육용 가상 IP**임을 부록 A와 코스 홈에 명시. JD PDF는 커밋 제외.

## 후속 가능 작업

- CI 빌드 결과 확인 후 필요 시 수정
- 선행 코스 `hbm`의 각 챕터 🔍 절에서 개별 항목 단위 딥링크 추가 (현재는 Ch06 종합표에만 연결)

---

# 사후 발견 결함 (2026-08-28, 클로즈아웃 이후)

## D2 SVG 누락으로 14개 페이지가 빈 본문으로 배포됨

**증상**: `hbm` 6개 + `hbm_dv` 8개 페이지가 제목·사이드바만 있고 **본문이 통째로 비어** 배포됨.

**원인**: `astro.config.mjs`의 `skipGeneration: !!process.env.CI` — D2 SVG는 **로컬에서 생성해 `public/d2/`에 커밋**하는 자산인데, 두 토픽 추가 시 이 자산을 커밋하지 않음.

```
[WARN] Skipping generation of D2 diagrams as the 'skipGeneration' option is enabled.
[ERROR] Failed to get D2 diagram size at '.../public/d2/docs/hbm/01_why_hbm-0.svg'
        ENOENT: no such file or directory
```

**왜 발견이 늦었나 — 세 겹의 실패**

| 겹 | 내용 |
|---|---|
| 1 | **로컬 빌드를 생략**했다. 그것이 검증 단계가 아니라 **필수 산출물 생성 단계**임을 몰랐다 (S19에서 "사용자 선호"로 기록했으나, 선호와 무관하게 필요한 작업이었음) |
| 2 | **Astro가 ERROR를 로그에 남기고도 빌드를 성공으로 종료** → CI가 초록 |
| 3 | 배포 확인이 **HTTP 200까지만** → 빈 페이지를 잡지 못함 |

**조치**: D2.js(WASM)로 15개 다이어그램 생성·커밋 (`7743992`). 전체 빌드는 로컬에서도 OOM(설정 주석에 기재된 알려진 특성)이라 대상 파일만 생성해 우회.

**부수 사고**: 중단된 빌드가 타 토픽 SVG 79개를 수정·삭제 → 전부 `git checkout`으로 원상 복구. 커밋에는 본 토픽 15개만 포함.

**검증**: CI 빌드 d2 오류 0건, 43개 페이지 본문 글자수 실측 — 빈 본문 0개 (2,465~21,266자).

## 재발 방지 (미적용 — 사용자 결정 대기)

- **A** — 대상 파일만 SVG를 생성하는 스크립트를 `scripts/`에 추가 (전체 빌드 OOM 회피)
- **B** — 배포 후 **본문 렌더 여부**(본문 글자수) 확인을 검증 절차에 추가. HTTP 200만으로는 부족함이 입증됨
