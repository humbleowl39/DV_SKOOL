# Plan: d2 다이어그램 가독성 결함 일괄 수정

## Objective
사용자가 지목한 `hbm4_jedec_dd/01`의 간선 라벨 겹침을 고치고, 같은 유형의 결함이
사이트 전체에 있는지 검사기로 전수 확인해 수정한다.

## Context
- 대상: `starlight_site/public/d2/docs/**` SVG 645개 (소스는 `src/content/docs/**` d2 블록)
- 검증 도구: 신규 `scripts/check_d2_overlap.mjs`, 기존 `scripts/check_d2_contrast.mjs`
- 시각 확인: `google-chrome --headless --screenshot` 으로 실제 렌더 확인

## 발견한 결함 2종

### ① 라벨 겹침 — 11개 파일
d2/elk는 도형 배치는 충돌 없이 풀지만 **간선 라벨은 서로의 폭을 고려하지 않고**
선 중앙에 놓는다. 평행/역평행 간선에 긴 라벨이 달리면 같은 높이에서 겹친다.

### ② 마크다운이 그대로 렌더 — 107개 파일 · 1,566곳
d2는 일반 라벨에서 마크다운을 처리하지 않는다(`|md |` 블록만 처리).
최소 예제로 확정: `A: "**Bold** plain"` → 화면에 `**Bold** plain`.
사용자가 지목한 다이어그램에도 `**Pseudo Channel 0**`이 별표째 나오고 있었다.

## Steps
- [x] S1 신규 검사기 `scripts/check_d2_overlap.mjs` 작성 (문자 종류별 폭 추정)
- [x] S2 전수 스캔 → 겹침 11건 확인, 최악 사례 렌더로 오탐 아님을 검증
- [x] S3 `hbm4_jedec_dd/01` 수정 — 동일 라벨 2개를 `PC = 0` / `PC = 1` 로 분리
- [x] S4 라벨 접기(`\n`)로 해결되는 8건 수정
- [x] S5 `ufs_hci/01` 재작성 — `grid-columns: 2` 제거, 컨테이너 구조로
- [x] S6 `mixed_signal_dv/07` 재작성 — `grid-rows: 2` 제거, 컨테이너 구조로
- [x] S7 d2 블록의 `**` 1,566개 제거 (107 파일)
- [x] S8 단일 마커(`*`/`_`/`` ` ``) 14건 수정, 오탐 2건 보존
- [x] S9 SVG 전량 재생성 + 겹침·대비 전수 재검사
- [x] S10 CI=true 빌드 · 커밋 · 배포 확인

## 보존해야 하는 것 (마크다운 아님)
- `rdma_verification/10`: `m_qp_tracker[*][*].pa_queue` — SV 연관배열 인덱싱
- `uvm/04_config_db_factory`: `'*.env.agent.*'` — config_db 경로 글롭

## Success Criteria
- 겹침 0건 · 대비 미달 0건
- d2 블록에 `**` 0개
- 재작성한 2개 다이어그램은 렌더 이미지로 가독성 확인

## Risks
- `gen_d2.mjs --all`은 645개 재생성 — 소요 시간 김
- 로컬 `npx astro build` 금지(= `public/d2` 파괴). 반드시 `CI=true`


---

# 종결 (Close-out)

## 최종 결과
```
겹침    4,994쌍 · 0건
대비   11,324건 · 0건
SVG 내 '**' / '&nbsp;' / 기타 HTML 엔티티 잔존 · 0
d2 재생성 662개 · 에러 0
```

## 계획 대비 편차

### 1. 결함이 1종이 아니라 4종이었다
지목받은 것은 라벨 겹침 하나였으나, 수정 과정에서 렌더 이미지를 확인하다 3종을 더 발견.

| | 결함 | 규모 |
|---|---|---|
| ① | 간선 라벨 겹침 | 11파일 (+ 후속 3건) |
| ② | `**` 가 별표로 노출 | 107파일 · 1,566곳 |
| ③ | `*` / `_` / 백틱 노출 | 14곳 |
| ④ | `&nbsp;` 가 글자로 노출 | 2파일 · 40곳 |

②③④는 공통 원인 — **d2 라벨을 마크다운/HTML처럼 작성**. d2는 `|md |` 블록에서만 처리한다.

### 2. 검사기 자체에 사각지대가 있었다 (가장 중요)
1차 검사기는 `<text>` 의 **직접 텍스트만** 파싱했다. 그런데 d2는 **여러 줄 라벨을 `<tspan>` 으로**
쪼개므로, 다중행 라벨이 통째로 검사 대상에서 빠져 있었다.
- 패치 전: 1,426쌍 검사 → "0건" (거짓 통과)
- 패치 후: 4,994쌍 검사 → 4건 발견
→ **교훈: 검사기가 0을 반환하면 먼저 "무엇을 세지 않았는가"를 의심한다.** 검사 대상 수(1,426 → 4,994)가
   그 신호였다.

### 3. `grid-*` 레이아웃이 겹침의 주범이었다
`ufs_hci/01`(`grid-columns: 2`), `mixed_signal_dv/07`(`grid-rows: 2`),
`hardware_interview/04`(`grid-rows: 3`) — **그리드는 간선을 고려하지 않고 노드를 배치**하므로
화살표가 전부 교차하고 라벨이 박스 위에 찍힌다. 셋 다 그리드 제거로 해결.
- `ufs_hci/01`은 Host/Device 열까지 뒤섞여 있었음 (그리드가 열 우선으로 채움)
- `mixed_signal_dv/07`은 컨테이너 이름 `g` 가 제목으로 노출되고 있었음

### 4. 라벨 접기가 문제를 옮기기도 한다
`hardware_interview/04` 에 `\n` 접기를 적용했더니 라벨이 다른 **노드 위로** 이동했다.
접기는 폭만 줄일 뿐 위치를 통제하지 못한다 → 근본 원인(그리드)을 제거해야 했다.

### 5. 일괄 치환에서 보존해야 했던 것
마크다운이 아니라 실제 문법인 2건. 기계적으로 밀었으면 깨졌다.
- `rdma_verification/10`: `m_qp_tracker[*][*].pa_queue` — SV 연관배열 인덱싱
- `uvm/04_config_db_factory`: `'*.env.agent.*'` — config_db 경로 글롭

## 신규 도구
`scripts/check_d2_overlap.mjs` — 문자 종류별 폭 추정으로 같은 줄 라벨 겹침 검출.
`<tspan>` 다중행 지원. 좁은 쪽 폭의 15% 이상 겹칠 때만 보고.

## 재발 방지 (미제안 상태)
- d2 라벨에 마크다운/HTML 금지 — `**`, `*`, 백틱, `&nbsp;` 는 그대로 렌더된다
- d2 에서 `grid-*` 는 간선 라벨을 망가뜨린다 — 흐름도에는 컨테이너 + elk 를 쓴다
- 로컬 `npx astro build` 금지 (= `public/d2` 파괴). 반드시 `CI=true`

---

# 추가 발견 (작업 중 파생)

## ⑤ 라이브 사이트에 백지 다이어그램이 있었다
`rdma_verification/01_tb_overview-3.svg` (UVM 환경 계층도) — **text 요소 0개, 완전한 백지**.
본 작업 이전부터 그 상태로 배포되고 있었다.

**원인**: `astro.config.mjs` 주석이 경고하던 버그가 실재.
d2 바이너리는 **4단 중첩 컨테이너**(`TOP > ENV > N0 > A0`)에서 **조용히 실패**한다 —
exit 0 을 반환하면서 파일을 아예 쓰지 않으므로, 옛 파일이 그대로 남고
`gen_d2.mjs` 는 "wrote" 라고 보고했다.

**조치 1 — 도구가 거짓 보고를 못 하게 막았다.** `gen_d2.mjs` 에 실패 검출 3종 추가:
- 파일 미생성
- mtime 미갱신 (조용한 no-op)
- 글자 없는 빈 SVG
→ 발견 시 `✗` 출력 + 최종 요약에 `⚠ 실패 N개` + `process.exit(1)`

**조치 2 — 다이어그램 평탄화.** `TOP` 컨테이너를 형제 노드로 분리해 4단 → 3단.
스크래치에서 사전 검증(text 0 → 18)한 뒤 적용.

## ⑥ d2 버전이 혼재해 있었다
HEAD 기준 `v0.7.0-HEAD` 532개 + `v0.7.1` 120개.
전자는 astro-d2 의 WASM 경로, 후자는 `gen_d2.mjs` 의 바이너리 경로 산출물.
전수 재생성으로 **652개 전부 v0.7.1 로 통일**했다.
→ SVG diff 594개 중 상당수는 이 정규화이며 내용 변경이 아니다.

# 이번 작업에서 도구가 세 번 거짓말했다

| # | 도구 | 증상 | 원인 | 신호였던 것 |
|---|---|---|---|---|
| 1 | 빌드 대기 루프 | 빌드 미완인데 "완료" | 종료 조건 `Error` 가 경고 문구 *"**Error** while highlighting"* 에 매칭 | `dist/` 가 비어 있었음 |
| 2 | `check_d2_overlap.mjs` | 겹침 있는데 "0건" | `<text>` 직접 텍스트만 파싱 — 다중행 라벨은 `<tspan>` 으로 쪼개져 누락 | 검사 쌍 1,426 (실제 5,114) |
| 3 | 평탄화 실험 계측 | 정상인데 "text=1" | `grep -c` 는 **줄 수**를 센다. SVG 는 전체가 한 줄 | 모든 변형이 똑같이 1 |

**공통 교훈: 검사기가 "이상 없음"을 반환하면 먼저 "무엇을 세지 않았는가"를 의심한다.**
세 번 모두 결과를 그대로 믿었으면 잘못 보고했을 것이고, 세 번 모두
**실제 렌더 이미지를 눈으로 확인**하는 과정에서 드러났다.

# 최종 검증
```
겹침       5,114쌍 · 0건
대비      11,360건 · 0건
빈 SVG            · 0개
d2 버전    652개 전부 v0.7.1
'**' / '&nbsp;'   · 0
```
